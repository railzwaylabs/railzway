export const idempotencyHint =
  "Optional. Provide a unique key to safely retry requests; repeats return the same response."

export const currencyHint = "Use ISO 4217 currency codes, e.g. USD, EUR."
